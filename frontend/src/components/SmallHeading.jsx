import React from "react";

const SmallHeading = ({text}) => {
    return(
        <div className="flex justify-center w-full p-1 text-xl font-semibold underline">
            {text}
        </div>
    )
}

export default SmallHeading
